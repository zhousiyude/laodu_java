1.一个java源文件中可以定义多个class
2.一个class会生成一个class文件。
3. public 的class是必须的嘛？不是。
4.public的class对应的类名必须和java源文件名保持一致。
5.public的class只能有一个。
6.任何一个类中都可以编写main方法。
7. 如果要运行A类中的main方法，则执行：javaA如果要运行T类中的main方法，则执行：java T

